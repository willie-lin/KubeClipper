package database

import (
	"testing"
)

//var csConfig	*csconfig.GlobalConfig

func TestNewClient(t *testing.T) {

	//_, err := NewClient(csConfig.DbConfig)
	////fmt.Sprint(dbClient)
	//if err != nil {
	//	log.Fatalf("unable to crate new database client: %s", err)
	//}
	//

}
